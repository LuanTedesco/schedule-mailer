<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f3f4f6;
        color: #1f2937;
        margin: 0;
        padding: 0;
      }

      .email-container {
        max-width: 1200px;
        margin: 40px auto;
        background-color: #ffffff;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 4px 14px rgba(0, 0, 0, 0.08);
      }

      .email-header {
        background: linear-gradient(90deg, #6366f1, #3b82f6);
        color: #ffffff;
        text-align: center;
        padding: 24px 20px;
        font-size: 22px;
        font-weight: bold;
      }

      .email-body {
        padding: 30px 24px;
        font-size: 16px;
        line-height: 1.7;
      }

      .email-body p {
        margin-bottom: 1em;
      }

      .email-footer {
        text-align: center;
        font-size: 13px;
        color: #6b7280;
        padding: 20px;
        background-color: #f9fafb;
      }

      a {
        color: #4f46e5;
        text-decoration: none;
      }

      @media (max-width: 600px) {
        .email-body {
          padding: 24px 16px;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <div class="email-header">
        Sistema Schedule Mailer
      </div>
      <div class="email-body">
        <p><strong>Enviado por:</strong> <%= @sender %></p>
        <p><%= simple_format(@body) %></p>
      </div>
      <div class="email-footer">
        Este e-mail foi enviado automaticamente. Não é necessário respondê-lo.<br />
        © <%= Time.current.year %> Schedule Mailer. Todos os direitos reservados.
      </div>
    </div>
  </body>
</html>
